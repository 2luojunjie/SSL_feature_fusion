# Data Directory

Put local speech emotion datasets here. This project does not download datasets automatically.

For RAVDESS, place the extracted dataset under:

```text
data/RAVDESS/
```

The parser recursively scans `.wav` files, so actor subdirectories are supported.
