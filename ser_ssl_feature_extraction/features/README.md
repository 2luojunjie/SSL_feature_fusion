# Features Directory

Extracted SSL features are saved here by default.

Each audio file is cached as one `.pt` file, and the extraction script also writes:

- `metadata.csv`
- `failed.txt` when one or more audio files fail
